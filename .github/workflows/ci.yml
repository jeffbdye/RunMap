name: PR

on:
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: ./.github/workflows/setup-repo.yml

    - uses: ./.github/workflows/setup-repo.yml
